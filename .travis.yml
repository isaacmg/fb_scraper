language: python
python:
  - "3.5"
cache: pip
services:
  - docker
  
install:
  - pip install -r requirements.txt
  - $TRAVIS_BUILD_DIR/kafk.sh
script:
  - coverage run tests/fb_posts_test.py
after_success:
  - codecov